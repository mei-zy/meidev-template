import { styled } from "styled-components";

const MainWrapper = styled.section`
  display: flex;
  align-items: center;

  .info {
    display: flex;
    margin: 0 auto;
  }
`;

const IndexStyle = {
  MainWrapper,
};

export default IndexStyle;
